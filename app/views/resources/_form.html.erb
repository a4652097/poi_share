<%= image_tag "logo.jpg", style:"width:100%;height:250px;"%>
<nav class="navbar navbar-default">
    <div class="container-fluid">
      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1" style="margin-left:25%;">
        <ul class="nav navbar-nav">
          <li class="active"><%= link_to '全部', resources_path%></li>
          <% @classifications.each do |c|%>
          <li id="<%= c.id%>">
          <a href="<%= resources_path(classification: c.id)%>"><%= c.name%></a>
          </li>
          <% end %>
        </ul>
        <form class="navbar-form navbar-left" role="search" style="margin-left: 43%;">
          <div class="form-group">
            <input type="text" class="form-control" placeholder="关键字搜索" name="keyword"  value="<%= params[:keyword]%>" >
          </div>
          <button type="submit" class="btn btn-default">搜索</button>
        </form>

      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>